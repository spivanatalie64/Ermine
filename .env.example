# Example environment variables for Ermine
# Copy to .env and fill with real secrets (never commit .env)

VAPID_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
FILES_ENCRYPTION_KEY=
LIVEKIT_KEY=
LIVEKIT_SECRET=

# Optional host overrides
APP_HOST=https://
API_HOST=https://
EVENTS_HOST=wss://
